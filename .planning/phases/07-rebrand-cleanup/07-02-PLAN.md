---
phase: 07-rebrand-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/beans.py
  - app/routers/brew.py
  - app/routers/insights.py
  - app/main.py
autonomous: true

must_haves:
  truths:
    - "_get_active_bean is defined in exactly one place and imported everywhere"
    - "Pending recommendations survive server restart"
    - "Invalid parameter override input shows user-facing error feedback"
    - "flavor_tags column migration is managed by Alembic, not startup code"
  artifacts:
    - path: "app/routers/beans.py"
      provides: "Canonical _get_active_bean helper"
      exports: ["_get_active_bean"]
    - path: "migrations/versions/"
      provides: "Alembic migration for flavor_tags column"
  key_links:
    - from: "app/routers/brew.py"
      to: "app/routers/beans.py"
      via: "import _get_active_bean"
      pattern: "from app.routers.beans import _get_active_bean"
    - from: "app/routers/insights.py"
      to: "app/routers/beans.py"
      via: "import _get_active_bean"
      pattern: "from app.routers.beans import _get_active_bean"
---

<objective>
Fix all 5 tech debt items from the v1 audit: deduplicate helper, persist pending recommendations, add Alembic migration, surface override parsing errors, and remove dead directory.

Purpose: Clean up code quality before public release.
Output: Debt-free codebase with all tests passing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deduplicate _get_active_bean and remove dead directory</name>
  <files>
    app/routers/beans.py
    app/routers/brew.py
    app/routers/insights.py
    app/routes/__init__.py
  </files>
  <action>
    1. **Deduplicate _get_active_bean:** The canonical definition is in `app/routers/beans.py` (L20). Remove the duplicate definitions from:
       - `app/routers/brew.py` (L33-39) — replace with `from app.routers.beans import _get_active_bean`
       - `app/routers/insights.py` (L23-29) — replace with `from app.routers.beans import _get_active_bean`
       
       Note: `analytics.py` and `history.py` already import from beans.py — those are correct.
    
    2. **Remove dead app/routes/ directory:** Delete `app/routes/` entirely (contains only an empty `__init__.py`). The actual routers live in `app/routers/`.
       Run: `rm -rf app/routes/`
    
    3. Run tests: `python -m pytest tests/ -v` to confirm no breakage.
  </action>
  <verify>`python -m pytest tests/ -v` passes. `grep -r "_get_active_bean" app/routers/ | grep "^.*:def " | wc -l` returns 1 (only beans.py defines it). `ls app/routes/` returns error (directory doesn't exist).</verify>
  <done>_get_active_bean defined once in beans.py, imported by brew.py, insights.py, analytics.py, history.py. Dead app/routes/ directory removed.</done>
</task>

<task type="auto">
  <name>Task 2: Persist pending recommendations and fix remaining debt</name>
  <files>
    app/routers/brew.py
    app/routers/beans.py
    app/main.py
    migrations/versions/
  </files>
  <action>
    1. **Persist pending_recommendations to file:** Replace in-memory `request.app.state.pending_recommendations` dict with file-based storage in the data directory. Use a simple JSON file (`{data_dir}/pending_recommendations.json`). Create a small helper module or inline functions:
       - `_save_pending(data_dir, rec_id, rec_dict)` — read file, add entry, write file
       - `_load_pending(data_dir, rec_id)` — read file, return entry or None
       - `_remove_pending(data_dir, rec_id)` — read file, remove entry, write file
       
       Update `brew.py`:
       - L110-113: Replace `request.app.state.pending_recommendations[rec_id] = rec` with `_save_pending(settings.data_dir, rec_id, rec)`
       - L129-130: Replace `getattr(request.app.state, "pending_recommendations", {})` with `_load_pending(settings.data_dir, recommendation_id)`
       - L244: Same pattern for the other `pending` lookup
       
       Import settings from `app.config` to get `data_dir`. This approach survives server restarts. Keep it simple — no database table needed for this temp state.
    
    2. **Move ALTER TABLE to Alembic migration:** Create a new Alembic migration for the `flavor_tags` column:
       - Run: `alembic revision -m "add_flavor_tags_to_measurements"` 
       - In the upgrade function: `op.add_column('measurements', sa.Column('flavor_tags', sa.Text(), nullable=True))`
       - In the downgrade function: `op.drop_column('measurements', 'flavor_tags')`
       - Remove the startup ALTER TABLE code from `app/main.py` (L29-36: the `sa_inspect` + `ALTER TABLE` block)
    
    3. **Surface error on invalid parameter override:** In `app/routers/beans.py` L179, replace `pass  # Skip invalid values` with logic that collects the invalid parameter names and returns an error response or flash message. Since we use htmx, a simple approach: add the invalid params to a list, and after the loop, if any exist, return a 400 response with a message like "Invalid values for: grind_size, temperature". This gives the user feedback instead of silently ignoring input.
    
    4. Run full test suite and update any broken tests for the new behavior.
  </action>
  <verify>
    - `python -m pytest tests/ -v` — all tests pass
    - `grep -r "pending_recommendations" app/main.py` returns no matches (no startup state)
    - `grep -r "ALTER TABLE" app/main.py` returns no matches
    - `ls migrations/versions/*flavor_tags*` shows new migration file
  </verify>
  <done>Pending recommendations persist to JSON file (survive restart). flavor_tags column managed by Alembic migration. Invalid override parsing returns user-visible error. All tests pass.</done>
</task>

</tasks>

<verification>
- All 5 tech debt items resolved:
  1. ✅ _get_active_bean deduplicated
  2. ✅ Dead app/routes/ removed
  3. ✅ pending_recommendations persisted to file
  4. ✅ ALTER TABLE moved to Alembic
  5. ✅ ValueError surfaced to user
- `python -m pytest tests/ -v` passes all tests
</verification>

<success_criteria>
- Zero tech debt items remaining from v1 audit
- All tests pass
- Recommendations survive simulated restart scenario
</success_criteria>

<output>
After completion, create `.planning/phases/07-rebrand-cleanup/07-02-SUMMARY.md`
</output>
