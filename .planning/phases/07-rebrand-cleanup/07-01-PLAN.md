---
phase: 07-rebrand-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/main.py
  - app/config.py
  - app/services/optimizer.py
  - app/templates/base.html
  - app/templates/beans/list.html
  - app/templates/beans/detail.html
  - app/templates/brew/index.html
  - app/templates/brew/recommend.html
  - app/templates/brew/best.html
  - app/templates/history/index.html
  - app/templates/insights/index.html
  - app/templates/analytics/index.html
  - tests/conftest.py
  - tests/test_brew.py
  - tests/test_beans.py
  - tests/test_history.py
autonomous: true

must_haves:
  truths:
    - "All UI pages show 'BeanBay' instead of 'BrewFlow'"
    - "Health endpoint returns service name 'beanbay'"
    - "All 108+ tests pass with updated naming"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package name 'beanbay'"
      contains: 'name = "beanbay"'
    - path: "app/config.py"
      provides: "BEANBAY_ env prefix"
      contains: "BEANBAY_"
    - path: "app/templates/base.html"
      provides: "BeanBay branding in nav and title"
      contains: "BeanBay"
  key_links:
    - from: "app/config.py"
      to: "docker-compose.yml"
      via: "env_prefix must match"
      pattern: "BEANBAY_"
    - from: "app/main.py"
      to: "tests/test_beans.py"
      via: "health check service name"
      pattern: "beanbay"
---

<objective>
Rename all references from BrewFlow to BeanBay across the entire codebase — Python code, HTML templates, config, and tests.

Purpose: Establish BeanBay as the project identity before public release.
Output: Fully rebranded codebase with all tests passing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename BrewFlow to BeanBay in all Python source files</name>
  <files>
    app/main.py
    app/config.py
    app/services/optimizer.py
    pyproject.toml
  </files>
  <action>
    Replace all occurrences of "BrewFlow" with "BeanBay" and "brewflow" with "beanbay" in:
    
    1. `pyproject.toml`: Change `name = "brewflow"` to `name = "beanbay"`, update description to mention BeanBay
    2. `app/main.py`: Update docstring ("BeanBay — FastAPI..."), FastAPI title ("BeanBay"), health endpoint service name ("beanbay")
    3. `app/config.py`: Change env_prefix from "BREWFLOW_" to "BEANBAY_", update db filename from "brewflow.db" to "beanbay.db", update docstring
    4. `app/services/optimizer.py`: Update docstring from "BrewFlow" to "BeanBay"
    
    IMPORTANT: The env_prefix change means BREWFLOW_DATA_DIR becomes BEANBAY_DATA_DIR. This must be consistent in Dockerfile and docker-compose.yml — but those are handled in Phase 9. For now, just update the Python side.
    
    Also rename the egg-info directory artifact: `rm -rf brewflow.egg-info/`
  </action>
  <verify>
    `grep -r "BrewFlow\|brewflow\|BREWFLOW" app/ pyproject.toml` returns zero matches (excluding __pycache__)
  </verify>
  <done>All Python source files and pyproject.toml reference "BeanBay"/"beanbay" instead of "BrewFlow"/"brewflow"</done>
</task>

<task type="auto">
  <name>Task 2: Rename BrewFlow to BeanBay in all templates and tests</name>
  <files>
    app/templates/base.html
    app/templates/beans/list.html
    app/templates/beans/detail.html
    app/templates/brew/index.html
    app/templates/brew/recommend.html
    app/templates/brew/best.html
    app/templates/history/index.html
    app/templates/insights/index.html
    app/templates/analytics/index.html
    tests/conftest.py
    tests/test_brew.py
    tests/test_beans.py
    tests/test_history.py
  </files>
  <action>
    1. Templates: Replace all "BrewFlow" with "BeanBay" in:
       - `base.html`: title default and nav-brand link text
       - All page templates: title blocks ("Insights — BeanBay", "Analytics — BeanBay", etc.)
    
    2. Tests: Update assertions and docstrings:
       - `tests/conftest.py`: Update docstring from "BrewFlow" to "BeanBay"
       - `tests/test_brew.py` L117: Change `assert "BrewFlow" in response.text` to `assert "BeanBay" in response.text`
       - `tests/test_history.py` L217: Change `assert "BrewFlow" not in response.text` to `assert "BeanBay" not in response.text`
       - `tests/test_beans.py` L64: Change `assert "BrewFlow" in response.text` to `assert "BeanBay" in response.text`
       - `tests/test_beans.py` L313: Change `"service": "brewflow"` to `"service": "beanbay"`
    
    3. Run full test suite: `python -m pytest tests/ -v`
  </action>
  <verify>`python -m pytest tests/ -v` — all 108+ tests pass. `grep -r "BrewFlow\|brewflow" app/templates/ tests/` returns zero matches.</verify>
  <done>All templates show "BeanBay" branding, all test assertions updated, full test suite passes</done>
</task>

</tasks>

<verification>
- `grep -ri "brewflow" app/ tests/ pyproject.toml` returns zero results
- `python -m pytest tests/ -v` passes all 108+ tests
- App title, nav brand, page titles all show "BeanBay"
</verification>

<success_criteria>
- Zero references to "BrewFlow" or "brewflow" in app/, tests/, or pyproject.toml
- All 108+ tests pass
- Health endpoint returns `{"status": "ok", "service": "beanbay"}`
</success_criteria>

<output>
After completion, create `.planning/phases/07-rebrand-cleanup/07-01-SUMMARY.md`
</output>
